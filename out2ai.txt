import{exec,spawn}from 'child_process';import * as vscode from 'vscode';import * as path from 'path';import{isDebugEnabled}from './utils';export interface FileVersion{id:string;ciBy:string;ciTime:string;changeSummary:string;}export interface FileStatus{type:string;state:string;change:string;lock:string;newRevision:string;revision:string;path:string;author:string;time:string;log:string;}interface StatusCacheEntry{status:FileStatus;timestamp:number;folderPath:string;}const statusCache = new Map<string,StatusCacheEntry>();const CACHE_TTL = 30000;function isCacheValid(entry:StatusCacheEntry):boolean{return Date.now()- entry.timestamp < CACHE_TTL;}export function getCachedStatus(filePath:string):FileStatus | null{const entry = statusCache.get(filePath);if(entry && isCacheValid(entry)){return entry.status;}return null;}export function setCachedStatus(filePath:string,status:FileStatus,folderPath:string):void{statusCache.set(filePath,{status,timestamp:Date.now(),folderPath});}export function clearFolderCache(folderPath:string):void{const keysToDelete:string[] = [];statusCache.forEach((entry,key)=>{if(entry.folderPath === folderPath){keysToDelete.push(key);}});keysToDelete.forEach(key => statusCache.delete(key));}export function clearAllCache():void{statusCache.clear();}export function escapePath(filePath:string):string{let escaped = filePath.replace(/"/g,'\\"');return `"${escaped}"`;}export function buildCommandArgs(baseArgs:string[],filePaths:string[]):string[]{const args = [...baseArgs];for(const filePath of filePaths){args.push(filePath);}return args;}export function executeSoscmd(args:string[],cwd?:string,showError?:boolean):Promise<string>;export function executeSoscmd(command:string,cwd?:string,showError?:boolean):Promise<string>;export function executeSoscmd(commandOrArgs:string | string[],cwd?:string,showError:boolean = true):Promise<string>{return new Promise((resolve,reject)=>{if(Array.isArray(commandOrArgs)){if(isDebugEnabled()){console.log(`[DEBUG] Executing soscmd with spawn:soscmd ${commandOrArgs.join(' ')}`);if(cwd){console.log(`[DEBUG] Working directory:${cwd}`);}vscode.window.showInformationMessage(`[DEBUG] Executing:soscmd ${commandOrArgs.join(' ')}${cwd ? `(cwd:${cwd})`:''}`);}const proc = spawn('soscmd',commandOrArgs,{cwd});let stdout = '';let stderr = '';proc.stdout.on('data',(data)=>{stdout += data.toString();if(isDebugEnabled()){console.log(`[DEBUG] stdout chunk:${data.toString()}`);}});proc.stderr.on('data',(data)=>{stderr += data.toString();if(isDebugEnabled()){console.log(`[DEBUG] stderr chunk:${data.toString()}`);}});proc.on('close',(code)=>{if(isDebugEnabled()){console.log(`[DEBUG] Process exited with code:${code}`);console.log(`[DEBUG] Full stdout:${stdout}`);if(stderr){console.log(`[DEBUG] Full stderr:${stderr}`);}}if(code !== 0){const errorMessage = `soscmd execution failed with exit code ${code}\nCommand:soscmd ${commandOrArgs.join(' ')}\nstdout:${stdout}\nstderr:${stderr}`;if(showError){console.error(`[ERROR] ${errorMessage}`);vscode.window.showErrorMessage(errorMessage);}else{console.log(`[DEBUG] Command failed(suppressed):soscmd ${commandOrArgs.join(' ')}`);}reject(new Error(errorMessage));return;}if(stderr){console.log(`[WARNING] Command completed with stderr:${stderr}`);}if(isDebugEnabled()){console.log(`[DEBUG] Command executed successfully`);}resolve(stdout);});proc.on('error',(error)=>{const errorMessage = `soscmd spawn error:${error.message}`;if(showError){console.error(`[ERROR] ${errorMessage}`);vscode.window.showErrorMessage(errorMessage);}else{console.log(`[DEBUG] Spawn error(suppressed):${error.message}`);}reject(new Error(errorMessage));});}else{const command = commandOrArgs;if(isDebugEnabled()){console.log(`[DEBUG] Executing soscmd:${command}`);if(cwd){console.log(`[DEBUG] Working directory:${cwd}`);}vscode.window.showInformationMessage(`[DEBUG] Executing:${command}${cwd ? `(cwd:${cwd})`:''}`);}exec(command,{cwd},(error,stdout,stderr)=>{let errorMessage = '';if(isDebugEnabled()){console.log(`[DEBUG] Command stdout:${stdout}`);if(stderr){console.log(`[DEBUG] Command stderr:${stderr}`);}}if(error){errorMessage = `soscmd execution failed:${error.message}`;if(cwd){errorMessage += `(Working directory:${cwd})`;}errorMessage += `\nCommand:${command}`;errorMessage += `\nstdout:${stdout}`;errorMessage += `\nstderr:${stderr}`;if(showError){console.error(`[ERROR] ${errorMessage}`);vscode.window.showErrorMessage(errorMessage);}else{console.log(`[DEBUG] Command failed(suppressed):${command}`);}reject(new Error(errorMessage));return;}if(stderr){console.log(`[WARNING] Command completed with stderr:${stderr}`);}if(isDebugEnabled()){console.log(`[DEBUG] Command executed successfully`);}resolve(stdout);});}});}export function parseStatusLine(statusLine:string):FileStatus | null{if(isDebugEnabled()){console.log(`[DEBUG] Parsing status line:"${statusLine}"`);}const trimmedLine = statusLine.trim();if(trimmedLine.length === 0){return null;}if(trimmedLine.includes('@@ Error')|| trimmedLine.includes('Error:')){if(isDebugEnabled()){console.log(`[DEBUG] Skipping error line:${trimmedLine}`);}return null;}const normalizedLine = trimmedLine.replace(/\t+/g,' ');if(normalizedLine.length >= 8){const statusCode = normalizedLine.substring(0,6);const validTypeChars = 'fpdFsS';const validStateChars = '-OWXN?';const validChangeChars = '-M!?';const validLockChars = '-L?';const validNewRevChars = '-N?';const validRsoChars = '-R?';const type = statusCode[0] || '-';const state = statusCode[1] || '-';const change = statusCode[2] || '-';const lock = statusCode[3] || '-';const newRevision = statusCode[4] || '-';const rsoMatch = statusCode[5] || '-';if(validTypeChars.includes(type)&& validStateChars.includes(state)&& validChangeChars.includes(change)){const restOfLine = normalizedLine.substring(6).trim();const parts = restOfLine.split(/\s+/);if(parts.length >= 2){const version = parts[0];const filePath = parts.slice(1).join(' ');const fileStatus:FileStatus ={type,state,change,lock,newRevision,revision:version,path:filePath,author:'',time:'',log:''};if(isDebugEnabled()){console.log(`[DEBUG] Fixed-width parsing result:${JSON.stringify(fileStatus)}`);}return fileStatus;}else if(parts.length === 1 && parts[0].length > 0){const fileStatus:FileStatus ={type,state,change,lock,newRevision,revision:parts[0],path:'',author:'',time:'',log:''};return fileStatus;}}}const patterns = [ /^([fpdFsS])([-OWXN?])([-M!?-])([-L?-])([-N?-])([-R?-])\s+(\d+)\s+(.+)$/,/^([fpdFsS])([-OWXN?])([-M!?-])([-L?-])([-N?-])([-R?-])\s+(\S+)\s+(.+)$/,/^([fpdFsS])([-OWXN?])([-M!?-])([-L?-])([-N?-])([-R?-])\s*(\S+)\s*(.+)$/ ];for(const pattern of patterns){const match = normalizedLine.match(pattern);if(match){const [,type,state,change,lock,newRevision,rsoMatch,version,filePath] = match;const fileStatus:FileStatus ={type,state,change,lock,newRevision,revision:version,path:filePath,author:'',time:'',log:''};if(isDebugEnabled()){console.log(`[DEBUG] Regex parsing result:${JSON.stringify(fileStatus)}`);}return fileStatus;}}const parts = normalizedLine.split(/\s+/);if(parts.length >= 3){if(isDebugEnabled()){console.log(`[DEBUG] Using fallback parsing with ${parts.length}parts:${parts.join('|')}`);}const statusCode = parts[0].padEnd(6,'-');const version = parts[1];const filePath = parts.slice(2).join(' ');const fileStatus:FileStatus ={type:statusCode[0] || '-',state:statusCode[1] || '-',change:statusCode[2] || '-',lock:statusCode[3] || '-',newRevision:statusCode[4] || '-',revision:version,path:filePath,author:'',time:'',log:''};if(isDebugEnabled()){console.log(`[DEBUG] Fallback parsing result:${JSON.stringify(fileStatus)}`);}return fileStatus;}if(isDebugEnabled()){console.log(`[DEBUG] Failed to parse status line:"${statusLine}"`);}return null;}export async function getFileStatus(filePath:string,useCache:boolean = true):Promise<FileStatus | null>{try{if(isDebugEnabled()){console.log(`[DEBUG] getFileStatus called with filePath:${filePath}`);}if(filePath.includes('sharedprocess')|| filePath.includes('vscode-extension-host')||(!filePath.includes('/')&& !filePath.includes(':'))){if(isDebugEnabled()){console.log(`[DEBUG] Skipping non-file:${filePath}`);}return null;}if(useCache){const cachedStatus = getCachedStatus(filePath);if(cachedStatus){if(isDebugEnabled()){console.log(`[DEBUG] Using cached status for:${filePath}`);}return cachedStatus;}}const fs = require('fs');if(!fs.existsSync(filePath)){if(isDebugEnabled()){console.log(`[DEBUG] File not found:${filePath}`);}return null;}const fileDir = path.dirname(filePath);const fileName = path.basename(filePath);const args = ['status',fileName];if(isDebugEnabled()){console.log(`[DEBUG] Building status command:soscmd ${args.join(' ')}`);console.log(`[DEBUG] Working directory:${fileDir}`);}const output = await executeSoscmd(args,fileDir,false);const lines = output.trim().split('\n');if(lines.length === 0){return null;}const statusLine = lines.find(line => line.trim().length > 0 && !line.includes('@@ Error'));if(!statusLine){return null;}if(statusLine.includes('Error:')){if(isDebugEnabled()){console.log(`[DEBUG] Error in status output:${statusLine}`);}return null;}const status = parseStatusLine(statusLine);if(status){setCachedStatus(filePath,status,fileDir);}return status;}catch(error){if(isDebugEnabled()){console.error(`[ERROR] getFileStatus failed:${error instanceof Error ? error.message:String(error)}`);}return null;}}export async function getFolderStatus(folderPath:string,useCache:boolean = true):Promise<Map<string,FileStatus>>{const statusMap = new Map<string,FileStatus>();try{if(isDebugEnabled()){console.log(`[DEBUG] getFolderStatus called with folderPath:${folderPath}`);}const fs = require('fs');if(!fs.existsSync(folderPath)){if(isDebugEnabled()){console.log(`[DEBUG] Folder not found:${folderPath}`);}return statusMap;}const args = ['status',folderPath];if(isDebugEnabled()){console.log(`[DEBUG] Building folder status command:soscmd ${args.join(' ')}`);console.log(`[DEBUG] Working directory:${folderPath}`);}const output = await executeSoscmd(args,folderPath,false);const lines = output.trim().split('\n');for(const line of lines){if(line.trim().length === 0 || line.includes('@@ Error')){continue;}if(line.includes('Error:')){if(isDebugEnabled()){console.log(`[DEBUG] Error in folder status output:${line}`);}continue;}const status = parseStatusLine(line);if(status && status.path){const fullPath = path.resolve(folderPath,status.path);statusMap.set(fullPath,status);if(useCache){setCachedStatus(fullPath,status,folderPath);}}}if(isDebugEnabled()){console.log(`[DEBUG] getFolderStatus returned ${statusMap.size}statuses`);}}catch(error){if(isDebugEnabled()){console.error(`[ERROR] getFolderStatus failed:${error instanceof Error ? error.message:String(error)}`);}}return statusMap;}export async function switchFileVersion(filePath:string,versionId:string):Promise<void>{try{if(isDebugEnabled()){console.log(`[DEBUG] switchFileVersion called with filePath:${filePath},versionId:${versionId}`);vscode.window.showInformationMessage(`[DEBUG] switchFileVersion:${filePath}-> v${versionId}`);}const fileDir = path.dirname(filePath);const fileName = path.basename(filePath);if(isDebugEnabled()){console.log(`[DEBUG] File directory:${fileDir}`);console.log(`[DEBUG] File name:${fileName}`);}const userevArgs = ['userev',`${filePath}/${versionId}`];if(isDebugEnabled()){console.log(`[DEBUG] Building userev command:soscmd ${userevArgs.join(' ')}`);}try{if(isDebugEnabled()){console.log(`[DEBUG] Attempting userev command...`);}await executeSoscmd(userevArgs,fileDir);vscode.window.showInformationMessage(`Successfully switched to version ${versionId}`);if(isDebugEnabled()){console.log(`[DEBUG] userev command executed successfully`);}}catch(error){if(isDebugEnabled()){console.error(`[ERROR] userev failed:${error instanceof Error ? error.message:String(error)}`);}if(error instanceof Error && error.message.includes('has been checked out. You must first check it in or cancel the checkout.')){const confirmMessage = `File ${fileName}is checked out. Do you want to discard changes and switch to version ${versionId}?`;if(isDebugEnabled()){console.log(`[DEBUG] File is checked out,asking user for confirmation...`);}const confirmResult = await vscode.window.showWarningMessage(confirmMessage,{modal:true},'Yes','No');if(confirmResult === 'Yes'){vscode.window.showWarningMessage(`Performing discard operation...`);if(isDebugEnabled()){console.log(`[DEBUG] User confirmed discard operation`);}const discardArgs = ['discard','-F',filePath];if(isDebugEnabled()){console.log(`[DEBUG] Building discard command:soscmd ${discardArgs.join(' ')}`);}await executeSoscmd(discardArgs,fileDir);if(isDebugEnabled()){console.log(`[DEBUG] discard command executed successfully`);}if(isDebugEnabled()){console.log(`[DEBUG] Retrying userev command after discard...`);}await executeSoscmd(userevArgs,fileDir);vscode.window.showInformationMessage(`Successfully switched to version ${versionId}after discard`);if(isDebugEnabled()){console.log(`[DEBUG] userev command executed successfully after discard`);}}else{if(isDebugEnabled()){console.log(`[DEBUG] User cancelled discard and version switch`);}vscode.window.showInformationMessage(`Version switch cancelled`);return;}}else{if(isDebugEnabled()){console.error(`[ERROR] userev failed with non-checkout error,rethrowing...`);}throw error;}}statusCache.delete(filePath);await refreshAllEditorsForFile(filePath);if(isDebugEnabled()){console.log(`[DEBUG] switchFileVersion completed successfully`);}}catch(error){const errorMsg = `Failed to switch version:${error instanceof Error ? error.message:String(error)}`;if(isDebugEnabled()){console.error(`[ERROR] switchFileVersion failed:${errorMsg}`);console.error(`[ERROR] Stack trace:${error instanceof Error ? error.stack:''}`);}vscode.window.showErrorMessage(errorMsg);}}async function refreshAllEditorsForFile(filePath:string):Promise<void>{const tabGroups = vscode.window.tabGroups;for(const group of tabGroups.all){for(const tab of group.tabs){if(tab.input instanceof vscode.TabInputText){const tabFilePath = tab.input.uri.fsPath;if(tabFilePath === filePath){try{const content = await vscode.workspace.fs.readFile(tab.input.uri);const newContent = Buffer.from(content).toString();const editor = vscode.window.visibleTextEditors.find(e => e.document.uri.fsPath === filePath);if(editor){const currentContent = editor.document.getText();if(currentContent !== newContent){const selection = editor.selection;const newLineCount = newContent.split('\n').length;const targetLine = Math.min(selection.active.line,newLineCount - 1);const targetLineLength = newContent.split('\n')[targetLine]?.length || 0;const targetChar = Math.min(selection.active.character,targetLineLength);await editor.edit(editBuilder =>{const fullRange = new vscode.Range(editor.document.positionAt(0),editor.document.positionAt(currentContent.length));editBuilder.replace(fullRange,newContent);});const newPosition = new vscode.Position(targetLine,targetChar);editor.selection = new vscode.Selection(newPosition,newPosition);if(isDebugEnabled()){console.log(`[DEBUG] Refreshed editor for ${filePath}(content changed)`);}}else{if(isDebugEnabled()){console.log(`[DEBUG] Skipped editor refresh for ${filePath}(content unchanged)`);}}}}catch(error){if(isDebugEnabled()){console.error(`[ERROR] Failed to refresh editor for ${filePath}:${error}`);}}}}}}}export async function getFileVersions(filePath:string):Promise<FileVersion[]>{try{if(isDebugEnabled()){console.log(`[DEBUG] getFileVersions called with filePath:${filePath}`);vscode.window.showInformationMessage(`[DEBUG] getFileVersions:${filePath}`);}const fileDir = path.dirname(filePath);const fileName = path.basename(filePath);if(isDebugEnabled()){console.log(`[DEBUG] File directory:${fileDir}`);console.log(`[DEBUG] File name:${fileName}`);}const args = ['history','-fs',filePath];if(isDebugEnabled()){console.log(`[DEBUG] Building command:soscmd ${args.join(' ')}`);}const output = await executeSoscmd(args,fileDir,false);if(output.includes('@@ Error:Client:No valid objects selected for \'history\' operation.')){if(isDebugEnabled()){console.log(`[DEBUG] File ${fileName}is not under SOS control`);vscode.window.showInformationMessage(`[DEBUG] File ${fileName}is not under SOS control`);}return [];}const versions:FileVersion[] = [];const lines = output.split('\n');if(isDebugEnabled()){console.log(`[DEBUG] Output lines count:${lines.length}`);console.log(`[DEBUG] Raw output:${output}`);}for(const line of lines){if(line.trim().startsWith('Action:checkin')){const versionMatch = line.match(/Revision:(\d+)\|.*?By:(.*?)\| At time:(\d{4}\/\d{2}\/\d{2}\d{2}:\d{2}:\d{2})\|.*?Log:(.*)$/);if(versionMatch){const [,id,ciBy,ciTime,changeSummary] = versionMatch;versions.push({id:id.trim(),ciBy:ciBy.trim(),ciTime:ciTime.trim(),changeSummary:changeSummary.trim()});if(isDebugEnabled()){console.log(`[DEBUG] Found version:ID=${id.trim()},By=${ciBy.trim()},Time=${ciTime.trim()}`);}}else{if(isDebugEnabled()){console.log(`[DEBUG] Line matched but failed to parse:${line}`);vscode.window.showWarningMessage(`[DEBUG] Failed to parse version line:${line}`);}}}}if(isDebugEnabled()){console.log(`[DEBUG] Found ${versions.length}versions`);vscode.window.showInformationMessage(`[DEBUG] Found ${versions.length}versions for ${fileName}`);}return versions;}catch(error){if(isDebugEnabled()){console.error(`[ERROR] getFileVersions failed:${error instanceof Error ? error.message:String(error)}`);console.error(`[ERROR] Stack trace:${error instanceof Error ? error.stack:''}`);}if(error instanceof Error && error.message.includes('No valid objects selected for \'history\' operation')){if(isDebugEnabled()){console.log(`[DEBUG] File is not under SOS control(from error)`);}return [];}const errorMsg = `Failed to get file versions:${error instanceof Error ? error.message:String(error)}`;vscode.window.showErrorMessage(errorMsg);return [];}}
import * as vscode from 'vscode';import * as path from 'path';import{FileStatus,getFolderStatus,getFileVersions,switchFileVersion,executeSoscmd,FileVersion,getFileStatus,clearAllCache,clearFolderCache}from './soscmd';import{isDebugEnabled,isCommandEnabled,getCommandConfig,replaceCommandVariables,isPlatformSupported,showPlatformWarning,getPathConfig,BATCH_SIZE,delay}from './utils';async function refreshFileStatus(filePaths:string[]):Promise<void>{for(const filePath of filePaths){try{clearFolderCache(path.dirname(filePath));if(fileStatusDecorator){await fileStatusDecorator.updateFileAndAncestors(filePath);}if(isDebugEnabled()){console.log(`[DEBUG] Completed file status refresh for ${filePath}`);}}catch(error){console.error(`[ERROR] Failed to refresh file status for ${filePath}:`,error);}}await vscode.commands.executeCommand('workbench.files.action.refreshFilesExplorer');}async function executeBatchCommand(filePaths:string[],fileDir:string,buildCommand:(batch:string[])=> string[],commandName:string):Promise<void>{if(filePaths.length <= BATCH_SIZE){const args = buildCommand(filePaths);console.log(`[DEBUG] ${commandName}command args:`,args);await executeSoscmd(args,fileDir);}else{const totalBatches = Math.ceil(filePaths.length / BATCH_SIZE);console.log(`[DEBUG] ${commandName}:Processing ${filePaths.length}files in ${totalBatches}batches`);for(let i = 0;i < filePaths.length;i += BATCH_SIZE){const batch = filePaths.slice(i,i + BATCH_SIZE);const batchNum = Math.floor(i / BATCH_SIZE)+ 1;const args = buildCommand(batch);console.log(`[DEBUG] ${commandName}batch ${batchNum}/${totalBatches}:`,args);try{await executeSoscmd(args,fileDir);vscode.window.showInformationMessage(`${commandName}batch ${batchNum}/${totalBatches}completed`);}catch(error){vscode.window.showErrorMessage(`${commandName}batch ${batchNum}/${totalBatches}failed:${error}`);throw error;}}}}class FileVersionItem extends vscode.TreeItem{public readonly version:FileVersion | null;public readonly filePath:string | null;public readonly isCurrent:boolean;constructor(version:FileVersion | null,filePath:string | null,isCurrent:boolean = false,collapsibleState:vscode.TreeItemCollapsibleState = vscode.TreeItemCollapsibleState.None){super(version ? version.id:'No versions',collapsibleState);this.version = version;this.filePath = filePath;this.isCurrent = isCurrent;if(version === null){this.tooltip = 'No versions available';this.description = '';this.contextValue = 'noVersions';this.iconPath = 'info';}else{this.tooltip = `Version ${version.id}- ${version.ciBy}- ${version.ciTime}\n${version.changeSummary}`;this.description = `${version.changeSummary}`;this.contextValue = 'fileVersion';if(isCurrent){this.iconPath = new vscode.ThemeIcon('check');this.description = 'Current';}else{this.iconPath = new vscode.ThemeIcon('circle-outline');}this.command ={title:'Switch to This Version',command:'cliosoft-sos-manager.switchVersion',arguments:[filePath,version]};}}}class FileVersionsTreeDataProvider implements vscode.TreeDataProvider<FileVersionItem>{private _onDidChangeTreeData:vscode.EventEmitter<FileVersionItem | undefined | null> = new vscode.EventEmitter<FileVersionItem | undefined | null>();readonly onDidChangeTreeData:vscode.Event<FileVersionItem | undefined | null> = this._onDidChangeTreeData.event;private currentFilePath:string | null = null;private currentFileStatus:FileStatus | null = null;refresh():void{this._onDidChangeTreeData.fire(null);}async setFile(filePath:string):Promise<void>{this.currentFilePath = filePath;this.currentFileStatus = await getFileStatus(filePath);if(isDebugEnabled()){console.log(`[DEBUG] File set to:${filePath}`);console.log(`[DEBUG] File status:${JSON.stringify(this.currentFileStatus)}`);}this.refresh();}getTreeItem(element:FileVersionItem):vscode.TreeItem{return element;}async getChildren(element?:FileVersionItem):Promise<FileVersionItem[]>{if(isDebugEnabled()){console.log(`[DEBUG] getChildren called with element:${element?.label}`);}if(!element){if(!this.currentFilePath){if(isDebugEnabled()){console.log('[DEBUG] No file selected,returning noVersions item');}return [new FileVersionItem(null,null,false)];}if(isDebugEnabled()){console.log(`[DEBUG] Active file:${this.currentFilePath}`);}const versions = await getFileVersions(this.currentFilePath);if(isDebugEnabled()){console.log(`[DEBUG] getFileVersions returned ${versions.length}versions`);}if(versions.length === 0){if(isDebugEnabled()){console.log(`[DEBUG] No versions found,returning noVersions item`);}return [new FileVersionItem(null,this.currentFilePath,false)];}if(isDebugEnabled()){console.log(`[DEBUG] Creating FileVersionItems for ${versions.length}versions`);}const currentRevision = this.currentFileStatus?.revision || '';return versions.map(version => new FileVersionItem(version,this.currentFilePath,version.id === currentRevision));}return [];}}class FileStatusDecorator implements vscode.Disposable{private statusCache:Map<string,FileStatus> = new Map();private updatingFolders:Set<string> = new Set();private readonly maxConcurrentUpdates = 5;private readonly decorationChangeEmitter = new vscode.EventEmitter<vscode.Uri[] | undefined>();private fileDecorationProvider:vscode.FileDecorationProvider;private fileDecorationProviderRegistration:vscode.Disposable | undefined;private isPaused:boolean = false;private statusBarItem:vscode.StatusBarItem;private disposables:vscode.Disposable[] = [];private folderUpdateTimestamps:Map<string,number> = new Map();private readonly folderUpdateInterval = 5000;constructor(){this.statusBarItem = vscode.window.createStatusBarItem('cliosoft-sos-manager.refreshToggle',vscode.StatusBarAlignment.Right,100);this.statusBarItem.text = '$(sync~spin)Refreshing...';this.statusBarItem.command = 'cliosoft-sos-manager.toggleRefresh';this.statusBarItem.tooltip = 'Click to pause/resume status refresh';this.statusBarItem.show();this.fileDecorationProvider ={provideFileDecoration:(uri:vscode.Uri)=>{const filePath = uri.fsPath;const status = this.statusCache.get(filePath);if(!status){return undefined;}let badge = '';let color = undefined;let tooltip = '';if(status.state === 'O' || status.state === 'W'){badge = '🔑';color = new vscode.ThemeColor('gitDecoration.modifiedResourceForeground');tooltip = 'Checked Out';if(status.change === 'M'){badge += '✏️';color = new vscode.ThemeColor('gitDecoration.modifiedResourceForeground');tooltip = 'Modified';}}else if(status.state === '-'){badge = '🔒';color = new vscode.ThemeColor('gitDecoration.untrackedResourceForeground');tooltip = 'Checked In(Locked)';}if(status.newRevision === 'N'){badge += '⚠️';color = new vscode.ThemeColor('gitDecoration.deletedResourceForeground');tooltip += '(Has New Revision)';}if(badge){return{badge,color,tooltip};}return undefined;},onDidChangeFileDecorations:this.decorationChangeEmitter.event};this.fileDecorationProviderRegistration = vscode.window.registerFileDecorationProvider(this.fileDecorationProvider);}toggleRefresh():void{this.isPaused = !this.isPaused;if(this.isPaused){this.statusBarItem.text = '$(debug-pause)Paused';this.statusBarItem.tooltip = 'Status refresh is paused. Click to resume.';}else{this.statusBarItem.text = '$(sync~spin)Refreshing...';this.statusBarItem.tooltip = 'Status refresh is active. Click to pause.';}}async updateFileAndAncestors(filePath:string):Promise<void>{if(this.isPaused){return;}if(!isPlatformSupported()){return;}try{const workspaceFolders = vscode.workspace.workspaceFolders;if(!workspaceFolders || workspaceFolders.length === 0){return;}const matchingWorkspaces = workspaceFolders.filter(folder => filePath.startsWith(folder.uri.fsPath));if(matchingWorkspaces.length === 0){return;}for(const workspaceFolder of matchingWorkspaces){await this.updateForWorkspace(filePath,workspaceFolder.uri.fsPath);}}catch(error){if(isDebugEnabled()){console.error(`[ERROR] Failed to update file and ancestors:${error instanceof Error ? error.message:String(error)}`);}}}private async updateForWorkspace(filePath:string,workspaceRoot:string):Promise<void>{const foldersToUpdate:string[] = [];let currentPath = path.dirname(filePath);while(currentPath && currentPath.length >= workspaceRoot.length){if(!path.basename(currentPath).startsWith('.')){foldersToUpdate.push(currentPath);}const parentPath = path.dirname(currentPath);if(parentPath === currentPath){break;}currentPath = parentPath;}foldersToUpdate.push(workspaceRoot);if(isDebugEnabled()){console.log(`[DEBUG] Updating ${foldersToUpdate.length}ancestor folders:${foldersToUpdate.join(',')}`);}const chunkSize = Math.min(this.maxConcurrentUpdates,foldersToUpdate.length);for(let i = 0;i < foldersToUpdate.length;i += chunkSize){const chunk = foldersToUpdate.slice(i,i + chunkSize);await Promise.all(chunk.map(async(folderPath)=>{await this.updateFolderStatus(folderPath);}));if(i + chunkSize < foldersToUpdate.length){await delay(200);}}}async updateFolderStatus(folderPath:string):Promise<void>{if(this.updatingFolders.has(folderPath)){return;}const lastUpdate = this.folderUpdateTimestamps.get(folderPath)|| 0;if(Date.now()- lastUpdate < this.folderUpdateInterval){if(isDebugEnabled()){console.log(`[DEBUG] Skipping folder ${folderPath}- updated recently`);}return;}this.updatingFolders.add(folderPath);try{const fs = require('fs');if(!fs.existsSync(folderPath)|| !fs.lstatSync(folderPath).isDirectory()){this.updatingFolders.delete(folderPath);return;}if(isDebugEnabled()){console.log(`[DEBUG] Getting status for folder:${folderPath}`);}const statusMap = await getFolderStatus(folderPath);this.folderUpdateTimestamps.set(folderPath,Date.now());if(isDebugEnabled()){console.log(`[DEBUG] Got ${statusMap.size}status entries from soscmd`);}const updatedPaths:string[] = [];statusMap.forEach((status,filePath)=>{const oldStatus = this.statusCache.get(filePath);if(!oldStatus || JSON.stringify(oldStatus)!== JSON.stringify(status)){this.statusCache.set(filePath,status);updatedPaths.push(filePath);}});if(updatedPaths.length > 0){const uris = updatedPaths .map(filePath =>{try{return vscode.Uri.file(filePath);}catch{return null;}}).filter((uri):uri is vscode.Uri => uri !== null);if(uris.length > 0){this.decorationChangeEmitter.fire(uris);}}}catch(error){if(isDebugEnabled()){console.error(`[ERROR] Failed to update folder status for ${folderPath}:${error instanceof Error ? error.message:String(error)}`);}}finally{this.updatingFolders.delete(folderPath);}}clearCache():void{this.statusCache.clear();this.folderUpdateTimestamps.clear();}dispose():void{this.decorationChangeEmitter.dispose();this.statusCache.clear();this.updatingFolders.clear();this.folderUpdateTimestamps.clear();if(this.fileDecorationProviderRegistration){this.fileDecorationProviderRegistration.dispose();this.fileDecorationProviderRegistration = undefined;}if(this.statusBarItem){this.statusBarItem.dispose();}this.disposables.forEach(d => d.dispose());}}let fileStatusDecorator:FileStatusDecorator | undefined;let isExtensionActivated = false;export function activate(context:vscode.ExtensionContext){if(!isPlatformSupported()){showPlatformWarning();isExtensionActivated = false;return;}isExtensionActivated = true;if(isDebugEnabled()){console.log('[DEBUG] ClioSoft SOS Manager extension activating...');vscode.window.showInformationMessage('[DEBUG] ClioSoft SOS Manager extension activating...');}const treeDataProvider = new FileVersionsTreeDataProvider();if(isDebugEnabled()){console.log('[DEBUG] Tree data provider created');}vscode.window.registerTreeDataProvider('cliosoft-sos-manager.fileVersions',treeDataProvider);if(isDebugEnabled()){console.log('[DEBUG] Tree view provider registered');}context.subscriptions.push(vscode.commands.registerCommand('cliosoft-sos-manager.refreshVersions',()=>{if(isDebugEnabled()){console.log('[DEBUG] Refresh versions command executed');vscode.window.showInformationMessage('[DEBUG] Refresh versions command executed');}treeDataProvider.refresh();}));if(isDebugEnabled()){console.log('[DEBUG] Refresh command registered');}context.subscriptions.push(vscode.commands.registerCommand('cliosoft-sos-manager.switchVersion',async(filePath:string | null,version:FileVersion | null)=>{if(!isExtensionActivated){vscode.window.showWarningMessage('SOS commands are disabled on this platform.');return;}if(isDebugEnabled()){console.log(`[DEBUG] Switch version command executed with filePath:${filePath},version:${version?.id}`);vscode.window.showInformationMessage(`[DEBUG] Switch version command:${filePath}-> v${version?.id}`);}if(filePath && version){if(isDebugEnabled()){console.log(`[DEBUG] Calling switchFileVersion for ${filePath}with version ${version.id}`);}await switchFileVersion(filePath,version.id);if(isDebugEnabled()){console.log(`[DEBUG] Re-fetching file status after version switch`);}await treeDataProvider.setFile(filePath);if(isDebugEnabled()){console.log(`[DEBUG] Refreshing tree after version switch`);}}else{console.error(`[ERROR] Invalid parameters for switchVersion:filePath=${filePath},version=${version}`);}}));if(isDebugEnabled()){console.log('[DEBUG] Switch version command registered');}context.subscriptions.push(vscode.commands.registerCommand('cliosoft-sos-manager.checkout',async(uri:vscode.Uri,uris:vscode.Uri[])=>{if(!isExtensionActivated){vscode.window.showWarningMessage('SOS commands are disabled on this platform.');return;}console.log('[DEBUG] Checkout command called with uri:',uri);console.log('[DEBUG] Checkout command called with uris:',uris);if(!isCommandEnabled('checkout')){console.log('[DEBUG] Checkout command is disabled');return;}const targetUris = uris || [uri];console.log('[DEBUG] Target uris:',targetUris);const filePaths = targetUris.map(u => u.fsPath);console.log('[DEBUG] File paths collected:',filePaths);if(filePaths.length === 0){console.log('[DEBUG] No file paths to process');return;}const fileDir = path.dirname(filePaths[0]);const fileNames = filePaths.map(function(p){return path.basename(p);}).join(',');console.log('[DEBUG] Working directory:',fileDir);console.log('[DEBUG] File names:',fileNames);await executeBatchCommand(filePaths,fileDir,(batch)=>{let customCommand = getCommandConfig('checkout');if(!customCommand){return ['co','-Nlock',...batch];}else{const replaced = replaceCommandVariables(customCommand,{filePath:batch});return replaced.split(/\s+/);}},'Checkout');vscode.window.showInformationMessage('Checked out:' + fileNames);console.log('[DEBUG] Checkout command completed successfully');await refreshFileStatus(filePaths);}));context.subscriptions.push(vscode.commands.registerCommand('cliosoft-sos-manager.checkin',async(uri:vscode.Uri,uris:vscode.Uri[])=>{if(!isExtensionActivated){vscode.window.showWarningMessage('SOS commands are disabled on this platform.');return;}console.log('[DEBUG] Checkin command called with uri:',uri);console.log('[DEBUG] Checkin command called with uris:',uris);if(!isCommandEnabled('checkin')){console.log('[DEBUG] Checkin command is disabled');return;}const targetUris = uris || [uri];console.log('[DEBUG] Target uris:',targetUris);const filePaths = targetUris.map(u => u.fsPath);console.log('[DEBUG] File paths collected:',filePaths);if(filePaths.length === 0){console.log('[DEBUG] No file paths to process');return;}const comments = await vscode.window.showInputBox({prompt:'Enter check-in comments',placeHolder:'Describe your changes...',validateInput:(value)=>{if(!value || value.trim().length === 0){return 'Comments cannot be empty';}return null;}});if(!comments){return;}const fileDir = path.dirname(filePaths[0]);const fileNames = filePaths.map(function(p){return path.basename(p);}).join(',');console.log('[DEBUG] Working directory:',fileDir);console.log('[DEBUG] File names:',fileNames);await executeBatchCommand(filePaths,fileDir,(batch)=>{let customCommand = getCommandConfig('checkin');if(!customCommand){return ['ci',`-aLog=${comments}`,...batch];}else{const replaced = replaceCommandVariables(customCommand,{filePath:batch,comments});return replaced.split(/\s+/);}},'Checkin');vscode.window.showInformationMessage('Checked in:' + fileNames);console.log('[DEBUG] Checkin command completed successfully');await refreshFileStatus(filePaths);}));context.subscriptions.push(vscode.commands.registerCommand('cliosoft-sos-manager.diff',async(uri:vscode.Uri)=>{if(!isExtensionActivated){vscode.window.showWarningMessage('SOS commands are disabled on this platform.');return;}if(!isCommandEnabled('diff')){return;}let filePath:string;if(uri){filePath = uri.fsPath;}else{const editor = vscode.window.activeTextEditor;if(!editor){return;}filePath = editor.document.uri.fsPath;}const fileDir = path.dirname(filePath);const fileName = path.basename(filePath);if(isDebugEnabled()){console.log(`[DEBUG] Diff command executed for:${filePath}`);}let customCommand = getCommandConfig('diff');let args:string[];if(!customCommand){args = ['diff','-gui',filePath];}else{const replaced = replaceCommandVariables(customCommand,{filePath});args = replaced.split(/\s+/);}await executeSoscmd(args,fileDir);}));context.subscriptions.push(vscode.commands.registerCommand('cliosoft-sos-manager.discard',async(uri:vscode.Uri,uris:vscode.Uri[])=>{if(!isExtensionActivated){vscode.window.showWarningMessage('SOS commands are disabled on this platform.');return;}console.log('[DEBUG] Discard command called with uri:',uri);console.log('[DEBUG] Discard command called with uris:',uris);if(!isCommandEnabled('discard')){console.log('[DEBUG] Discard command is disabled');return;}const targetUris = uris || [uri];console.log('[DEBUG] Target uris:',targetUris);const filePaths = targetUris.map(u => u.fsPath);console.log('[DEBUG] File paths collected:',filePaths);if(filePaths.length === 0){console.log('[DEBUG] No file paths to process');return;}const selectedOption = await vscode.window.showQuickPick([{label:'Yes(discard all changes)',value:true},{label:'No(keep local changes)',value:false}],{placeHolder:'Do you want to use -F parameter to discard all changes?',title:'Discard Changes'});if(!selectedOption){return;}const useForce = selectedOption.value;const fileDir = path.dirname(filePaths[0]);const fileNames = filePaths.map(function(p){return path.basename(p);}).join(',');console.log('[DEBUG] Working directory:',fileDir);console.log('[DEBUG] File names:',fileNames);await executeBatchCommand(filePaths,fileDir,(batch)=>{let customCommand = getCommandConfig('discard');if(!customCommand){const args = ['discard'];if(useForce){args.push('-F');}args.push(...batch);return args;}else{const replaced = replaceCommandVariables(customCommand,{filePath:batch,useForce:useForce ? '-F':''});return replaced.split(/\s+/);}},'Discard');vscode.window.showInformationMessage('Discarded:' + fileNames);console.log('[DEBUG] Discard command completed successfully');await refreshFileStatus(filePaths);}));context.subscriptions.push(vscode.commands.registerCommand('cliosoft-sos-manager.officeOpen',async(uri:vscode.Uri)=>{if(!isExtensionActivated){vscode.window.showWarningMessage('SOS commands are disabled on this platform.');return;}if(!isCommandEnabled('officeOpen')){return;}let filePath:string;if(uri){filePath = uri.fsPath;}else{const editor = vscode.window.activeTextEditor;if(!editor){return;}filePath = editor.document.uri.fsPath;}const fileDir = path.dirname(filePath);if(isDebugEnabled()){console.log(`[DEBUG] Office open command executed for:${filePath}`);}let customCommand = getCommandConfig('officeOpen');let args:string[];if(!customCommand){args = ['soffice',filePath];}else{const replaced = replaceCommandVariables(customCommand,{filePath});args = replaced.split(/\s+/);}await executeSoscmd(args,fileDir);}));context.subscriptions.push(vscode.commands.registerCommand('cliosoft-sos-manager.rebuildCtags',async(uri:vscode.Uri)=>{if(!isExtensionActivated){vscode.window.showWarningMessage('SOS commands are disabled on this platform.');return;}if(!isCommandEnabled('rebuildCtags')){return;}let filePath:string;if(uri){filePath = uri.fsPath;}else{const editor = vscode.window.activeTextEditor;if(!editor){return;}filePath = editor.document.uri.fsPath;}const fileDir = path.dirname(filePath);if(isDebugEnabled()){console.log(`[DEBUG] Rebuild ctags command executed for:${filePath}`);}let customCommand = getCommandConfig('rebuildCtags');let command:string;if(!customCommand){const rtlPath = getPathConfig('rtl')|| './design_data/rtl';const testbenchPath = getPathConfig('testbench')|| './design_data/testbench';const refIpPath = getPathConfig('refIp')|| './ref_ip';const tagsOutputPath = getPathConfig('tagsOutput')|| '.vscode/.tags';command = `cd \${env:PROJ_ROOT};ctags -R --fields=+nKz -f ${tagsOutputPath}--langmap=SystemVerilog:+.v+.sv -R --links=yes ${rtlPath}${testbenchPath}${refIpPath}`;}else{command = replaceCommandVariables(customCommand,{filePath});}await executeSoscmd(command,fileDir);}));context.subscriptions.push(vscode.commands.registerCommand('cliosoft-sos-manager.toggleRefresh',()=>{if(fileStatusDecorator){fileStatusDecorator.toggleRefresh();}}));fileStatusDecorator = new FileStatusDecorator();context.subscriptions.push(vscode.window.onDidChangeActiveTextEditor((editor)=>{if(editor && editor.document && isExtensionActivated){fileStatusDecorator?.updateFileAndAncestors(editor.document.uri.fsPath);treeDataProvider.setFile(editor.document.uri.fsPath);}}));context.subscriptions.push(vscode.workspace.onDidSaveTextDocument((document)=>{if(isExtensionActivated){fileStatusDecorator?.updateFileAndAncestors(document.uri.fsPath);}}));context.subscriptions.push(vscode.workspace.onDidOpenTextDocument((document)=>{if(isExtensionActivated){fileStatusDecorator?.updateFileAndAncestors(document.uri.fsPath);}}));if(vscode.window.tabGroups && vscode.window.tabGroups.onDidChangeTabs){let lastActiveTab:string | undefined;const tabGroupsListener = vscode.window.tabGroups.onDidChangeTabs(()=>{if(!isExtensionActivated){return;}try{const activeTab = vscode.window.tabGroups.activeTabGroup?.activeTab;if(activeTab && activeTab.input){let filePath:string | undefined;if((activeTab.input as any).uri){filePath =(activeTab.input as any).uri.fsPath;}else if((activeTab.input as any).viewType){const input = activeTab.input as any;if(input.uri){filePath = input.uri.fsPath;}}if(filePath && filePath !== lastActiveTab){lastActiveTab = filePath;fileStatusDecorator?.updateFileAndAncestors(filePath);treeDataProvider.setFile(filePath);if(isDebugEnabled()){console.log(`[DEBUG] Active tab changed to:${filePath}`);}}}}catch(error){if(isDebugEnabled()){console.error(`[ERROR] Failed to check active tab:`,error);}}});context.subscriptions.push(tabGroupsListener);}context.subscriptions.push(vscode.workspace.onDidChangeWorkspaceFolders(()=>{if(fileStatusDecorator){fileStatusDecorator.clearCache();}clearAllCache();}));context.subscriptions.push(vscode.window.onDidChangeWindowState((windowState)=>{if(windowState.focused && isExtensionActivated){const activeEditor = vscode.window.activeTextEditor;if(activeEditor && activeEditor.document){fileStatusDecorator?.updateFileAndAncestors(activeEditor.document.uri.fsPath);}}}));if(isDebugEnabled()){console.log('[DEBUG] ClioSoft SOS Manager extension activated!');vscode.window.showInformationMessage('[DEBUG] ClioSoft SOS Manager extension activated!');}}export function deactivate(){console.log('ClioSoft SOS Manager extension deactivated!');if(fileStatusDecorator){fileStatusDecorator.dispose();fileStatusDecorator = undefined;}clearAllCache();isExtensionActivated = false;}
import * as vscode from 'vscode';export const DEBUG_INFO_CONFIG_KEY = 'cliosoft-sos-manager.enableDebugInfo';export function isDebugEnabled():boolean{return vscode.workspace.getConfiguration().get<boolean>(DEBUG_INFO_CONFIG_KEY,false);}export function getSelectedFilePath(context:any):string | null{if(context && context.fsPath){return context.fsPath;}const activeEditor = vscode.window.activeTextEditor;if(activeEditor){return activeEditor.document.uri.fsPath;}return null;}export function getConfig(){return vscode.workspace.getConfiguration('cliosoft-sos-manager');}export function isCommandEnabled(commandName:string):boolean{const config = getConfig();return config.get(`commands.${commandName}.enable`,true);}export function getCommandConfig(commandName:string):string{const config = getConfig();return config.get(`commands.${commandName}.command`,'');}export function getPathConfig(pathName:string):string{const config = getConfig();return config.get(`paths.${pathName}`,'');}export function replaceCommandVariables(command:string,variables:Record<string,string | string[]>):string{let result = command;for(const [key,value] of Object.entries(variables)){if(Array.isArray(value)){const escapedValue = value.map(v => `"${v.replace(/"/g,'\\"')}"`).join(' ');result = result.replace(new RegExp(`\\$\\{${key}\\}`,'g'),escapedValue);}else{result = result.replace(new RegExp(`\\$\\{${key}\\}`,'g'),value);}}return result;}export function isPlatformSupported():boolean{return process.platform === 'linux';}export async function showPlatformWarning():Promise<void>{const message = 'ClioSoft SOS Manager is designed to run on Linux only. ' + 'All SOS commands are disabled on this platform.';const result = await vscode.window.showWarningMessage(message,'Learn More','Dismiss');if(result === 'Learn More'){vscode.env.openExternal(vscode.Uri.parse('https:);}}export const BATCH_SIZE = 50;export function delay(ms:number):Promise<void>{return new Promise(resolve => setTimeout(resolve,ms));}export async function isFileUnderSosControl(filePath:string):Promise<boolean>{try{const fs = require('fs');if(!fs.existsSync(filePath)){return false;}const fileDir = require('path').dirname(filePath);const sosConfigPath = require('path').join(fileDir,'.sos');return fs.existsSync(sosConfigPath);}catch{return false;}}

# 2026-08-13

## SOS Manager Extension session memory update

- Recorded v0.45.0 session outcomes for ClioSoft SOS Manager.
- Key product changes this session:
  - Added `SOS create file` right-click command using `soscmd create <file_path>`.
  - Added manual Changed Files full-scan progress and success/failure notifications.
  - Fixed unsupported global scan selector `-snr`; current selector is `soscmd status * -sco -suco -sncm -sne -snt`.
  - Preserved user-confirmed checkout syntax `soscmd co -Nlock <file>` after reverting a mistaken `-N lock` change.
  - Reduced status debug log flooding by summarizing large stdout and removing per-line successful parse logs.
  - Stopped single-file operations from automatically running expensive full-workspace Changed Files scan; global scan remains manual.
- Important diagnosis: checkout itself may be fast while perceived UI delay comes from later status scan/rebuild/logging. User logs showed `co -Nlock` at 184 ms, but global status scan at 2188 ms and later 16441 ms.
- Updated `memory/topics/sos-manager-extension.md` and core memory pointers/lessons.
- No unrelated workspace changes should be staged by memory maintenance.

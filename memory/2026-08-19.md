# 2026-08-19

## SOS Manager v0.47 Diff + release

- Implemented the approved Diff plan against official `soscmd help diff`.
- Multi-file Diff resolves Explorer / Changed Files multi-select and runs `soscmd diff -gui <file>` once per file, with progress/cancel/counts. Different files are never the two sides of one `soscmd diff`.
- Added **Diff Two SOS Revisions**: 1 picked revision → workarea/checkout vs `file/#/rev`; 2 picked revisions → `file/#/rev1` vs `file/#/rev2`.
- Custom diff vars: `${filePath}`, `${filePath1}`, `${filePath2}`, `${revision1}`, `${revision2}`.
- User then asked `/servu-upload` with “更新版本号，打包并上传到服务器”. Bumped `0.46.0` → `0.47.0`, packaged `cliosoft-sos-manager-0.47.0.vsix` (16 files, 175.95 KB), uploaded to `IC_Design_Public/haokai.xiong` (1/1). Do not print upload credentials.
- Upload path that worked: `C:\Users\haokai.xiong\AppData\Local\Programs\Python\Python313\python.exe` + `.claude/skills/servu-upload/scripts/upload.py`. `vsce` is `/d/tool_env/node/vsce`.
- Leftover unchanged: Changed Files visibility listener still auto-runs `refreshFilteredStatus` when the view is visible, empty, and has no disk cache. Direct full-scan call is still only that command (`src/extension.ts` ~954–987).

## Collaboration notes

- Treat pasted `soscmd help` as the source of truth. `diff` is same-file revision compare (at most two pathnames of the same file), not a general A-vs-B comparer.
- `/servu-upload` plus “更新版本号” means bump + package + upload in one flow; do not wait for a second confirmation.
